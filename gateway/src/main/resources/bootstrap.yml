info:
    app:
        name: ${spring.application.name}
logging:
    level:
        org:
            springframework:
                cloud:
                    gateway: debug
        root: info
management:
    endpoint:
        health:
            show-details: always
    endpoints:
        jmx:
            exposure:
                include: '*'
        web:
            exposure:
                include: '*'
spring:
    application:
        name: gateway
    cloud:
        kubernetes:
            config:
                enabled: true
                name: gateway-config
                namespace: default
            reload:
                enabled: true
                mode: event
                strategy: refresh
